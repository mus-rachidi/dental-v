<UserControl x:Class="ClinicManager.Views.Pages.ReportsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <Border Width="48" Height="48" Margin="0,0,0,12" HorizontalAlignment="Center"
                    Background="{DynamicResource InfoBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1">
                <TextBlock Text="&#xE9F9;" FontFamily="Segoe MDL2 Assets" FontSize="24"
                           Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center" />
            </Border>
            <TextBlock Text="Reports &amp; Analytics" FontSize="20" FontWeight="Bold"
                       Foreground="{DynamicResource TextPrimaryBrush}" HorizontalAlignment="Center" Margin="0,0,0,6" />
            <TextBlock Text="View financial reports, patient statistics, and analytics" FontSize="12"
                       Foreground="{DynamicResource TextSecondaryBrush}" HorizontalAlignment="Center" Margin="0,0,0,20" />
            <Border Style="{DynamicResource BadgeStyle}" Background="{DynamicResource WarningLightBrush}" 
                    HorizontalAlignment="Center" Padding="12,4">
                <TextBlock Text="Coming Soon" FontSize="11" FontWeight="SemiBold"
                           Foreground="{DynamicResource WarningBrush}" />
            </Border>
        </StackPanel>
    </Grid>
</UserControl>
